"""Private shared base for LangChain-based integrations.

This package is **not** a user-facing integration. It hosts the generic
``langchain-core`` callback handler that other integrations
(``langgraph``, ``langchain``, future ``deepagents``) subclass.
"""
