# Don't auto-install optional peer dependencies (the heavy agent frameworks).
# Base `make ts-install` stays lean; install each framework on demand via the
# `make ts-install-<framework>` targets (the analog of Python's `uv sync --extra`).
auto-install-peers=false
